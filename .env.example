# Copy this file to `.env` in development and set real values.
# NEVER commit real secret keys to source control.

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=service_role_XXXXXXXXXXXXXXXXXXXXXXXX

# Resend
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXX
RESEND_FROM=no-reply@aurium.site

# Notes:
# - Use the Service Role Key only on trusted server-side code (serverless functions).
# - Set these variables in your hosting provider (Vercel/Netlify) for production.